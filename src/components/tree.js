export default function tree(p5) {
  // trees.rex += Math.random(-0.5, 0.5);    
  // trees.rey -= Math.random(3);            
  // w = Math.abs(trees.maxSize / (-trees.rey + 25))+5;        
  // a = Math.map(-trees.rey, trees.maxSize, 0, 0, 200);        
  // p5.strokeWeight(12);         
  // p5.stroke('#fcfcfc', 12);
  p5.fill('#acacac')
    p5.stroke('#acacac')
    p5.ellipse(400, 100, 50)
}
